name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Test EC2 connection
        run: |
          echo "🔍 Testing connection to EC2..."
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_PUBLIC_IP }} << 'EOF'
            echo "✅ GitHub → EC2 연결 성공!" > /home/ubuntu/connection_test.txt
            echo "📂 현재 위치: $(pwd)" >> /home/ubuntu/connection_test.txt
            echo "🕒 작성 시각: $(date)" >> /home/ubuntu/connection_test.txt
            ls -al ~ >> /home/ubuntu/connection_test.txt
          EOF
